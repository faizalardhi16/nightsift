# Backend: NestJS

Guidelines for NestJS (TypeScript) repositories:
- Follow existing module/controller/service structure.
- Use dependency injection consistent with the codebase.
- Match existing validation and DTO patterns.
- Run `npm run build` and `npm test` before finishing.
