# Requirement Readiness

Evaluate whether a task has enough information to implement without guessing.

Criteria (application-scored, not self-reported):
- objective, acceptance_criteria, repository, module_context,
  expected_behavior, edge_cases, test_expectations, dependencies, target_branch.

Be strict and evidence-based. Never invent facts.
