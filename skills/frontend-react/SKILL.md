# Frontend: React

Guidelines for React repositories:
- Follow existing component/hook patterns.
- Keep styling consistent with the codebase.
- Run the configured lint, build, and test commands before finishing.
