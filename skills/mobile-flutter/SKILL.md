# Mobile: Flutter

Guidelines for Flutter (Dart) repositories:
- Follow existing widget/state-management patterns.
- Keep UI changes minimal and consistent with the design system.
- Run `flutter analyze` and `flutter test` before finishing.
- Do not modify generated files (e.g. *.g.dart) by hand.
