# Backend: Node.js

Guidelines for Node.js (TypeScript/JavaScript) repositories:
- Follow existing module patterns and lint rules.
- Run the configured build, lint, and test commands before finishing.
